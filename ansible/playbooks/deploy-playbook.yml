- hosts: kong-api-server
  become: true
  tasks:
  - name: Create Repo in /opt/collin/kong-service
    copy:
      src: ../../api
      dest: /opt/collin/kong-service/
      owner: root
      group: root
      mode: 0640
  
  - name: Start Kong-Api 
    shell: cd /opt/collin/kong-service/api; npm i; nohup npm start &
